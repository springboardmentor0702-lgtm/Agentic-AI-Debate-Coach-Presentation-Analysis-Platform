{
  "name": "logos-ai-backend",
  "version": "4.0.0",
  "description": "Logos.ai FastAPI Backend Wrapper",
  "scripts": {
    "dev": "uvicorn backend.main:app --reload --port 8000",
    "start": "uvicorn backend.main:app --host 0.0.0.0 --port 8000"
  }
}
